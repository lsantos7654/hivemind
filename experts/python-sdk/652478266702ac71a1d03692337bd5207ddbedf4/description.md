Expert on the python-sdk repository — the official Python implementation of the Model Context Protocol (MCP), covering both server and client development with the `mcp` PyPI package. This expert handles questions about building MCP servers using the `FastMCP` high-level decorator API (`@mcp.tool()`, `@mcp.resource()`, `@mcp.prompt()`), the low-level `Server` class for direct handler registration, and MCP client development with `ClientSession`, `ClientSessionGroup`, `stdio_client`, `sse_client`, and `streamable_http_client`. It covers all transport layers (stdio, SSE, Streamable HTTP, WebSocket), OAuth 2.1 authorization via `OAuthAuthorizationServerProvider` and `TokenVerifier`, the elicitation API (`Context.elicit()`, `ElicitationResult`), sampling (`CreateMessageRequest`, `SamplingFnT`), structured tool output with `outputSchema`, the experimental Tasks API (`tasks/get`, `tasks/result`, `tasks/cancel`), DNS rebinding protection via `TransportSecuritySettings`, and in-memory testing with `create_connected_server_and_client_session`. Automatically invoked for questions about `FastMCP`, `ClientSession`, `ServerSession`, `BaseSession`, `RequestResponder`, `ToolManager`, `ResourceManager`, `PromptManager`, `Context.elicit`, `StreamableHTTPSessionManager`, `EventStore`, `AuthSettings`, `OAuthAuthorizationServerProvider`, `mcp run`, `mcp dev`, `mcp install`, `FASTMCP_*` environment variables, `mcp.types`, `CallToolResult`, `ContentBlock`, or the `mcp[cli]` / `mcp[ws]` extras.
