Expert on the httpx repository — a next-generation HTTP client for Python 3 that provides both synchronous and asynchronous APIs with full HTTP/1.1 and HTTP/2 support. Covers the `httpx.Client` and `httpx.AsyncClient` class APIs, top-level convenience functions (`get`, `post`, `put`, `patch`, `delete`, `head`, `options`, `stream`, `request`), the `Request` and `Response` data models with their properties (`status_code`, `headers`, `text`, `content`, `json()`, `iter_bytes()`, `iter_lines()`, `elapsed`, `raise_for_status()`), the `Headers` case-insensitive multi-dict, `Cookies` jar wrapping `http.cookiejar.CookieJar`, `URL` parsing with IDNA support, authentication schemes (`BasicAuth`, `DigestAuth`, `NetRCAuth`, `FunctionAuth`), transport layer (`HTTPTransport`, `AsyncHTTPTransport`, `ASGITransport`, `WSGITransport`, `MockTransport`), configuration classes (`Timeout`, `Limits`, `Proxy`, `create_ssl_context`), exception hierarchy (`HTTPError`, `RequestError`, `TimeoutException`, `HTTPStatusError`, `StreamError`), content decoders (gzip, deflate, brotli, zstandard), multipart uploads, event hooks, redirect handling, and the `httpx` command-line client built with click and rich. Automatically invoked for questions about httpx, `httpx.Client`, `httpx.AsyncClient`, `httpx.Headers`, `httpx.Request`, `httpx.Response`, `httpx.URL`, `httpx.URL.__init__`, `httpx.HTTPTransport`, `httpx.Timeout`, `httpx.HTTPError`, `httpx.HTTPStatusError`, `httpx.ASGITransport`, `httpx.WSGITransport`, `httpx.MockTransport`, `httpx.BasicAuth`, `httpx.DigestAuth`, `httpx.codes`, `httpx.QueryParams`, `httpx.Proxy`, `httpx.Limits`, `httpx.create_ssl_context`, and `httpcore` integration.