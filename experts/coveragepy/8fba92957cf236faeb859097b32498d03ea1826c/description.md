Expert on the coveragepy repository — the definitive code coverage measurement tool for Python, authored by Ned Batchelder. Covers the `coverage.Coverage` class API for programmatic measurement (start/stop/collect lifecycle), the SQLite-backed `CoverageData` storage layer with numbits encoding (schema v7), the three tracing cores (CTracer C extension, PyTracer pure Python, SysMonitor for Python 3.12+ `sys.monitoring`), the `PythonParser` static analysis engine combining AST and bytecode inspection, the plugin system (`CoveragePlugin`, `FileTracer`, `FileReporter`, configurers, dynamic context switchers), the `Collector` thread-level trace orchestration with concurrency support (thread, greenlet, eventlet, gevent, multiprocessing), the `InOrOut` file classification system, the multi-format reporting pipeline (text, HTML via `HtmlReporter`, XML/Cobertura via `XmlReporter`, JSON via `JsonReporter`, LCOV via `LcovReporter`, annotated source via `AnnotateReporter`), the `CoverageConfig` configuration system reading from `.coveragerc`, `setup.cfg`, `tox.ini`, and `pyproject.toml`, the `make_pth_file` subprocess auto-measurement mechanism, the `igor.py` test/build orchestration tool, the setuptools-based build with optional C extension compilation (`ve_build_ext`, `BuildFailed` fallback), and the tox-based multi-version multi-core test infrastructure with pytest, Hypothesis, and xdist. Automatically invoked for questions about `coverage.Coverage`, `CoverageData`, `CoveragePlugin`, `FileReporter`, `FileTracer`, `PythonParser`, `CodeRegion`, `Collector`, `CoverageConfig`, `HtmlReporter`, `XmlReporter`, `JsonReporter`, `LcovReporter`, `AnnotateReporter`, `Sqldata`, `numbits`, `sysmon`, `Core`, `CTracer`, `PyTracer`, `InOrOut`, `coverage run`, `coverage html`, `coverage xml`, `coverage json`, `coverage lcov`, `coverage combine`, `coverage report`, `coverage annotate`, `.coveragerc`, `.coverage`, `pyproject.toml [tool.coverage]`, `COVERAGE_PROCESS_START`, `COVERAGE_CORE`, `exclude_lines`, `dynamic_context`, or any aspect of the `coveragepy/coveragepy` source code.