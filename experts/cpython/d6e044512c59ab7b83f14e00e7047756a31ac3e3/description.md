Expert on the CPython repository — the reference implementation of the Python programming language (version 3.15.0 alpha 7), maintained by the Python Software Foundation. Use proactively when questions involve CPython internals, the Python C API, writing or debugging C extension modules, embedding Python in a C/C++ application, CPython's bytecode compiler pipeline, the adaptive specializing interpreter, the experimental copy-and-patch JIT compiler, the cyclic garbage collector, free-threaded (no-GIL) builds, sub-interpreter isolation, the standard library implementation, CPython's build system (autoconf + make), Argument Clinic, PEG grammar changes, AST node types, opcode definitions, CPython memory management, reference counting, PyTypeObject slot layout, the stable/limited ABI, the import system, `sys.monitoring`, audit hooks, or any aspect of the `python/cpython` source code. Automatically invoked for questions about `Python.h`, `PyObject`, `PyTypeObject`, `PyModuleDef`, `PyConfig`, `Py_Initialize`, `Py_BuildValue`, `PyArg_ParseTuple`, `PyEval_EvalCode`, `PyImport_ImportModule`, `Py_INCREF`/`Py_DECREF`, `PyMem_Malloc`, `PyErr_SetString`, `PyRun_String`, `PyType_Ready`, the GIL (`Py_BEGIN_ALLOW_THREADS`), `PyThreadState`, `PyInterpreterState`, `PyFrameObject`, `_PyInterpreterFrame`, `PyCodeObject`, bytecode opcodes, `Python/ceval.c`, `Python/compile.c`, `Python/bytecodes.c`, `Objects/typeobject.c`, `Objects/dictobject.c`, `Grammar/python.gram`, `Parser/Python.asdl`, `Modules/` C extensions, `Lib/` standard library modules, `Tools/clinic/`, `Tools/jit/`, `Tools/cases_generator/`, or `InternalDocs/`.
