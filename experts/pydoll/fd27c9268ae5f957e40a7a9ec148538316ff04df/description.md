Expert on the Pydoll repository (`autoscrape-labs/pydoll`, PyPI: `pydoll-python`) — an async-native, WebDriver-free Python library for automating Chromium-based browsers (Chrome and Edge) via the Chrome DevTools Protocol (CDP) over WebSocket. Use proactively when questions involve Pydoll browser automation, CDP-based browser control without WebDriver, the `Chrome`/`Edge` browser classes, the `Tab` class and its navigation/element-finding/JS-execution methods, `WebElement` interaction, `FindElementsMixin` (`find()`, `query()`, `query_all()`), the Pydantic-powered `ExtractionModel`/`Field` extraction engine, `tab.extract()`/`tab.extract_all()`, humanized mouse movement with Bezier curves and Fitts's Law, Shadow DOM access including closed shadow roots, `tab.find_shadow_roots()`, CDP event subscription with `tab.on()`, network request interception via `FetchEvent.REQUEST_PAUSED`, network monitoring, HAR recording/replay, hybrid UI + API automation with `tab.request`, browser contexts and isolated sessions, multi-tab concurrency, remote/Docker browser connections via WebSocket, Cloudflare Turnstile auto-bypass, browser fingerprint control with `ChromiumOptions.browser_preferences`, the `@retry` decorator, `ConnectionHandler` WebSocket management, `ChromiumOptions` configuration, or any aspect of the `autoscrape-labs/pydoll` source code. Automatically invoked for questions about `from pydoll.browser import Chrome`, `async with Chrome() as browser`, `tab.go_to()`, `tab.find()`, `tab.query()`, `tab.execute_script()`, `tab.extract()`, `tab.extract_all()`, `ExtractionModel`, `Field(selector=...)`, `tab.mouse`, `tab.keyboard`, `tab.scroll`, `tab.request.get()`, `tab.request.record()`, `tab.on(FetchEvent.REQUEST_PAUSED, ...)`, `tab.enable_fetch_events()`, `tab.find_shadow_roots()`, `element.get_shadow_root()`, `tab.enable_auto_solve_cloudflare_captcha()`, `tab.save_bundle()`, `ChromiumOptions`, `@retry`, `ConnectionHandler`, `pydoll-python`, or `pydoll.tech` documentation.
