- Pydoll library architecture and design patterns
- `Chrome` and `Edge` browser class instantiation and lifecycle management (`pydoll/browser/chromium/`)
- `Browser` abstract base class methods: `start()`, `stop()`, `connect()`, `new_tab()`, `get_opened_tabs()`, `create_browser_context()`, `delete_browser_context()`, `get_cookies()`, `set_cookies()`, `set_download_behavior()`, `grant_permissions()`, `on()`, `enable_fetch_events()`, `continue_request()`, `fail_request()`, `fulfill_request()`
- `Tab` class (primary user interface) and all its methods (`pydoll/browser/tab.py`)
- Tab navigation: `go_to()`, `refresh()`, `current_url`, `title`, `page_source`
- Tab event management: `enable_page_events()`, `enable_network_events()`, `enable_fetch_events()`, `enable_dom_events()`, `enable_runtime_events()` and corresponding `disable_*` methods
- JavaScript execution via `tab.execute_script()` (both page-level and element-level)
- Screenshot capture: `take_screenshot()` with format, quality, beyond_viewport options
- PDF generation: `print_to_pdf()` with print options
- Page bundle saving: `save_bundle()` with `inline_assets` option
- Cookie management: `get_cookies()`, `set_cookies()`, `delete_all_cookies()`
- Dialog handling: `has_dialog()`, `get_dialog_message()`, `handle_dialog()`
- File download configuration and `wait_download()`
- Cloudflare Turnstile auto-bypass: `enable_auto_solve_cloudflare_captcha()`
- Shadow DOM traversal: `tab.find_shadow_roots(deep=True, timeout=n)`
- OOPIF (cross-origin iframe) shadow root discovery
- `FindElementsMixin` API: `find()`, `query()`, `query_all()`, `wait_element()` with all parameter combinations
- `find()` keyword-based element selection (id, class_name, name, tag_name, text, custom attributes)
- `query()` / `query_all()` CSS selector and XPath auto-detection
- `WebElement` class methods: `click()`, `double_click()`, `right_click()`, `type_text()`, `insert_text()`, `press_keyboard_key()`, `clear_input()`, `scroll_into_view()`, `scroll_to_bottom()`, `get_attribute()`, `tag_name`, `attributes`, `is_visible()`, `is_on_top()`, `is_interactive()`, `get_outer_html()`, `get_inner_text()`, `get_shadow_root()`, `take_screenshot()`, `set_input_files()`
- `ShadowRoot` class and shadow DOM interaction (including closed mode shadow roots)
- Pydantic `ExtractionModel` / `Field` API (`pydoll/extractor/`)
- `Field()` parameters: `selector`, `attribute`, `description`, `default`, `transform`
- `tab.extract()` and `tab.extract_all()` with `scope` and `timeout`
- Nested `ExtractionModel` composition
- CSS vs XPath auto-detection in selectors
- `ExtractionEngine` internals (`pydoll/extractor/engine.py`)
- Humanized mouse movement physics: Bezier curves, Fitts's Law, minimum-jerk velocity, tremor, overshoot (`pydoll/interactions/mouse.py`, `utils.py`)
- `MouseAPI`: `move()`, `click()`, `double_click()`, `right_click()`, `drag()` with `humanize` flag
- `KeyboardAPI`: `press()`, `key_down()`, `key_up()`, `type_text()`
- `ScrollAPI`: `up()`, `down()`, `left()`, `right()`, `to_top()`, `to_bottom()`
- `Key` enum values for keyboard input
- `tab.request` HTTP API: `get()`, `post()`, `put()`, `delete()` methods
- HAR recording: `tab.request.record()` context manager, `capture.save()`, `tab.request.replay()`
- `Response` class: `json()`, `text()`, `bytes()`, `status_code`, `headers`
- Network interception patterns with `FetchEvent.REQUEST_PAUSED`
- `continue_request()`, `fail_request()`, `fulfill_request()` for request modification
- Auth challenge handling: `FetchEvent.AUTH_REQUIRED` and `continue_with_auth()`
- Network monitoring: `enable_network_events()`, `get_network_logs()`, `get_network_response_body()`
- `ChromiumOptions` configuration: `headless`, `binary_location`, `start_timeout`, `page_load_state`, `webrtc_leak_protection`
- `ChromiumOptions.add_argument()`, `remove_argument()`
- `ChromiumOptions.browser_preferences` for fingerprint management
- `ChromiumOptions` convenience setters: `block_notifications`, `block_popups`, `password_manager_enabled`, `prompt_for_download`, `allow_automatic_downloads`, `open_pdf_externally`
- `@retry` decorator: `max_retries`, `exceptions`, `on_retry`, `delay`, `exponential_backoff`, `exception_to_raise`
- `ConnectionHandler` WebSocket lifecycle, `execute_command()`, event callback registration
- `EventsManager` network log accumulation and dialog state tracking
- CDP domain structure in `pydoll/protocol/` (fetch, network, dom, page, runtime, storage, target, etc.)
- CDP event enums: `PageEvent`, `FetchEvent`, `NetworkEvent`, `DomEvent`
- `pydoll/commands/` command builder classes and methods
- Exception hierarchy: all 30+ exception classes and when they are raised
- `By` enum for element selection strategies
- `PageLoadState` enum: COMPLETE, INTERACTIVE, LOADING
- `SelectorParser` CSS vs XPath auto-detection logic
- `IFrameContext` and `IFrameContextResolver` for cross-origin iframe routing
- `UserAgentParser` and `Emulation.setUserAgentOverride` integration
- SOCKS5 proxy forwarding (`pydoll/utils/socks5_proxy_forwarder.py`)
- Proxy configuration and per-context proxy credential management
- Multi-tab concurrency with `asyncio.gather`
- Browser context isolation (cookies, localStorage separation)
- Remote browser connections and WebSocket URL handling (query-string token preservation)
- `Tab.bring_to_front()`, `Tab.close()`
- Page event patterns: page load waiting, `_wait_page_load()` context manager
- File chooser dialog interception: `enable_intercept_file_chooser_dialog()`, `expect_file_chooser()`
- `save_bundle()` internals: `collect_frame_resources`, `filter_fetchable_resources`, `inline_all_assets`, `rewrite_html_urls`
- `TempDirectoryManager`, `BrowserProcessManager`, `ProxyManager` internals
- `py.typed` PEP 561 marker and mypy integration
- Project build system: Poetry, pyproject.toml, taskipy tasks (`lint`, `format`, `test`)
- Test structure and pytest configuration
- Ruff linting configuration and code style rules
- CHANGELOG and version history (v2.22.1, Python >= 3.10 requirement)
