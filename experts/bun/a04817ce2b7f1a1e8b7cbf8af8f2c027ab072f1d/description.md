Expert on the bun repository — the all-in-one JavaScript/TypeScript toolkit written in Zig and powered by JavaScriptCore, covering the runtime, bundler, package manager, test runner, HTTP server, and all built-in APIs. This expert covers `Bun.serve`, `Bun.file`, `Bun.write`, `Bun.spawn`, `Bun.spawnSync`, `Bun.connect`, `Bun.listen`, `Bun.udpSocket`, `Bun.build`, `Bun.Transpiler`, `Bun.Glob`, `Bun.CryptoHasher`, `Bun.password`, `Bun.semver`, `Bun.color`, `Bun.CSRF`, `Bun.Terminal`, `Bun.sql`, `Bun.redis`, `Bun.valkey`, `Bun.s3`, `bun:sqlite`, `bun:ffi`, `bun:test`, the `Bun.$` shell scripting API, the Bake full-stack dev server, the `bun install`/`bun add`/`bun remove` package manager, `bun test`, `bun build`, `bunx`, `bunfig.toml`, the binary lockfile (`bun.lockb`), the text lockfile (`bun.lock`), Node.js compatibility layer, plugin API, macros, single-file executables, and the CMake/Zig/LLVM build system. Automatically invoked for questions about `Bun.serve`, `ServerWebSocket`, `bun:sqlite`, `bun:ffi`, `bun:test`, `Bun.$`, `Bun.build`, `Bun.Transpiler`, `VirtualMachine.zig`, `BundleV2`, `PackageManager.zig`, `Lockfile`, `HTMLRewriter`, `FileSystemRouter`, `Bun.S3Client`, `Bun.ValkeyClient`, `bunfig.toml`, `bun.lockb`, `bun.lock`, `bunx`, `bun upgrade`, or any `oven-sh/bun` source-level question.
