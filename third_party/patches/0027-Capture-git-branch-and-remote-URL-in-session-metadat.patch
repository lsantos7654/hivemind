From 5b53695592e8aab9ffda1632ff3d6d2453167cab Mon Sep 17 00:00:00 2001
From: Lucas Santos <lsantos7654@gmail.com>
Date: Mon, 11 May 2026 13:55:35 -0400
Subject: [PATCH 27/32] Capture git branch and remote URL in session metadata
 at creation

Read .git/HEAD and .git/config (origin remote) via synchronous
filesystem I/O in createNext so every session carries its git
context. Handles worktrees (gitdir: pointer files) and falls
back silently on non-git directories / unexpected formats.
---
 packages/opencode/src/session/index.ts | 36 ++++++++++++++++++++++++++
 1 file changed, 36 insertions(+)

diff --git a/packages/opencode/src/session/index.ts b/packages/opencode/src/session/index.ts
index 7801efc..c6b1e07 100644
--- a/packages/opencode/src/session/index.ts
+++ b/packages/opencode/src/session/index.ts
@@ -1,5 +1,6 @@
 import { Slug } from "@opencode-ai/util/slug"
 import path from "path"
+import { readFileSync } from "fs"
 import { BusEvent } from "@/bus/bus-event"
 import { Bus } from "@/bus"
 import { Decimal } from "decimal.js"
@@ -392,6 +393,37 @@ export namespace Session {
       const config = yield* Config.Service
       const scope = yield* Scope.Scope
 
+      const readGitInfo = Effect.fnUntraced("Session.readGitInfo")(function* (dir: string) {
+        return yield* Effect.sync(() => {
+          const output: Record<string, unknown> = {}
+          try {
+            let root = path.join(dir, ".git")
+            let head: string
+            try {
+              head = new TextDecoder().decode(readFileSync(path.join(root, "HEAD")))
+            } catch {
+              const text = new TextDecoder().decode(readFileSync(root))
+              const match = text.match(/^gitdir:\s*(.+)$/m)
+              if (!match) return output
+              root = path.resolve(dir, match[1])
+              head = new TextDecoder().decode(readFileSync(path.join(root, "HEAD")))
+            }
+            head = head.trim()
+            if (head.startsWith("ref: refs/heads/")) {
+              output.git_branch = head.slice(16)
+            } else if (head.length >= 7) {
+              output.git_branch = head.slice(0, 7)
+            }
+            try {
+              const cfg = new TextDecoder().decode(readFileSync(path.join(root, "config")))
+              const m = cfg.match(/\[remote\s+"origin"\][^\[]*url\s*=\s*(.+)/s)
+              if (m) output.git_remote_url = m[1].trim()
+            } catch {}
+          } catch {}
+          return output
+        })
+      })
+
       const createNext = Effect.fn("Session.createNext")(function* (input: {
         id?: SessionID
         title?: string
@@ -423,6 +455,10 @@ export namespace Session {
             updated: Date.now(),
           },
         }
+        const git = yield* readGitInfo(input.directory)
+        if (Object.keys(git).length > 0) {
+          result.metadata = { ...input.metadata, ...git }
+        }
         log.info("created", result)
 
         yield* Effect.sync(() => SyncEvent.run(Event.Created, { sessionID: result.id, info: result }))
-- 
2.50.1

