You are writing a section for a team lead agent definition. The team is "{{ team_name }}".

Read the expert's knowledge documentation at:
- {{ expert_dir }}/summary.md
- {{ expert_dir }}/agent.md
- {{ expert_dir }}/apis_and_interfaces.md
- {{ expert_dir }}/code_structure.md

Then write ONLY the following to stdout (no file writes):

## expert-{{ expert_name }}

[Write a substantial, detailed paragraph (8-15 sentences) describing what this expert covers, its core capabilities, key APIs and patterns it knows about, and specific scenarios when the orchestrator should consult it. Be concrete — mention actual class names, modules, CLI commands, or architectural patterns. This section is used by the team lead to make routing decisions, so it needs enough detail to distinguish this expert from others on the team.]

Write ONLY the ## heading and paragraph. No frontmatter, no extra sections.
