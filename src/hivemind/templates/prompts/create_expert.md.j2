You are performing a deep analysis of the repository at {{ repo_dir }} to create expert knowledge documentation for the "{{ name }}" expert.

The commit is {{ commit }}. Write all files into {{ commit_dir }}/.

Generate these 6 files:

1. **{{ commit_dir }}/summary.md** (500-800 words)
   - Repository purpose and goals
   - Key features and capabilities
   - Primary use cases and target audience
   - High-level architecture overview
   - Related projects and dependencies

2. **{{ commit_dir }}/code_structure.md** (1000-1500 words)
   - Complete annotated directory tree
   - Module and package organization
   - Main source directories and their purposes
   - Key files and their roles
   - Code organization patterns

3. **{{ commit_dir }}/build_system.md** (800-1200 words)
   - Build system type and configuration files
   - External dependencies and management
   - Build targets and commands
   - How to build, test, and deploy

4. **{{ commit_dir }}/apis_and_interfaces.md** (1000-1500 words)
   - Public APIs and entry points
   - Key classes, functions, and macros
   - Usage examples with code snippets
   - Integration patterns and workflows
   - Configuration options and extension points

5. **{{ commit_dir }}/description.md** — A single paragraph (no headings, no bullets, plain prose, ~3–6 sentences) used as the expert's agent description. Open with "Expert on the <project> repository — <one-line characterization>." Then list specific, concrete topics, scenarios, and trigger phrases that should invoke this expert. Mention canonical class names, modules, CLI commands, file formats, or APIs the expert covers, so the orchestrator can match user questions to this expert. End with a sentence beginning "Automatically invoked for questions about ..." that lists the most distinctive identifiers (function names, configuration keys, package names) tied to this repository.

6. **{{ commit_dir }}/expertise.md** — A comprehensive bullet list (no `## Expertise` heading — bullets only, ~100–150 lines) enumerating what this expert knows. Group bullets under bold sub-topic labels (e.g. `### Core Architecture`, `### APIs`, `### Build Configuration`). Each entry should be specific: name actual classes, functions, files, or features. Avoid generic phrasing like "uses good patterns" — be concrete. This content goes verbatim under the `## Expertise` heading of the deployed agent.

**Important formatting rules:**
- `description.md` must contain ONLY the description paragraph. No leading heading, no frontmatter.
- `expertise.md` must contain ONLY the bullet list / sub-section bullets. No leading `## Expertise` heading, no frontmatter.
- Both files are inputs to a Jinja template that wraps them with the rest of the agent definition (workflow instructions, anti-hallucination rules, constraints) at deploy time.

Analyze the repository thoroughly using Read, Grep, and Glob tools before writing. Explore the directory structure, key source files, build files, and documentation. Write comprehensive, accurate documentation based on actual code inspection.
